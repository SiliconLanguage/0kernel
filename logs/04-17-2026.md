# 0kernel Work Log - 04-17-2026

## Scope Of Work Completed

### Research Section — PDF Artifact Integration
- Created `/public/research/` directory to host downloadable PDF briefs.
- Added `/research/*` pass-through rule to `_redirects` so Cloudflare serves all files under that path directly.
- Wired up per-card PDF `href` links in `Artifacts.tsx` for all three research briefs:
  - `brief-001` → `Artifact_Interception_Integrity-Double-Trampoline.pdf` — label: `[ VIEW_TECHNICAL_BRIEF ]`
  - `brief-002` → `Monadic_Data_Plane_Architecture_GPUDirect_Update.pdf` — label: `[ OPEN_ARCH_SPEC ]`
  - `brief-003` → `domain-specific-compilation.pdf` — label: `[ READ_RESEARCH_PAPER ]`
- Added `(RESTRICTED_FULL_DECK_AVAILABLE_ON_REQUEST)` notice to `brief-001` (Double Trampoline card).
- Added `.artifact-link` CSS class: JetBrains Mono, `#00f2ff`, uppercase, hover brightness + CRT flicker reuse.

### Foundry Card — Initial Implementation
- Added fourth card `FOUNDRY-001` to the `[ 04_RESEARCH ]` section as a static element (not in the `ARTIFACTS` array).
- Grid changed from `lg:grid-cols-3` to `sm:grid-cols-2` for a clean 2×2 layout.
- RECORDS FOUND counter updated to `ARTIFACTS.length + 1` (displays 4).
- Card features: circuit-node SVG icon, `LIVE` badge, `SOURCE_CODE / OPEN_BUILD / ACTIVE` tags.
- Added `.foundry-card` CSS with `@keyframes foundry-pulse` (2.8s breathing inset cyan border).
- Link: `https://github.com/SiliconLanguage` with `target="_blank" rel="noopener noreferrer"`.

### Foundry Card — Final Siliconization Revision
- Updated subtitle: **"Engineering the Monadic Paradigm."** — styled Monadic Cyan (`#00f2ff`), bold.
- Updated description to Siliconization narrative: "Bridging the data plane from emulation to the physical substrate..."
- Replaced circuit-node icon with a minimalist silicon lattice SVG (4×4 grid + center nucleus).
- Updated tag from `SOURCE_CODE` → `SILICON_SYNTHESIS`.
- Added `.foundry-scanline` overlay with continuous 8s vertical drift (data-stream effect).
- Added `border-trace` hover animation: cyan 1px border traces card perimeter (top → right → bottom → left, 2.4s loop).
- All card content wrapped in `relative z-10` to layer above scanlines and trace animations.

### Build Configuration
- Updated `package.json` build script to explicitly copy `/public/research/` into `/dist/research/` after `vite build`:
  ```
  tsc && vite build && mkdir -p dist/research && cp -r public/research/* dist/research/
  ```
- Verified Vite already handles `public/` → `dist/` automatically; explicit copy acts as a safety net.

### Cleanup
- Removed Windows `Zone.Identifier` sidecar files from `/public/research/` (NTFS alternate data stream metadata).
- Added `*:Zone.Identifier` glob to `.gitignore` to prevent recurrence.
- Removed Zone.Identifier entries from git index (they had been committed in a prior session).

### Identity.tsx
- Kept manual edit: `AI/HPC` → `AI, HPC` punctuation fix in the Identity bio section.

## Deployments
- Version `edb0b035-b4ca-48cf-8949-2f58ebc34a72` — Research PDFs live, Foundry card v1
- Version `b8385da7-be98-4d2a-8faf-2662ced2b316` — Foundry card final revision deployed

## Commits
- `feat(research)`: PDF artifact links, RESTRICTED notice, Foundry card v1, CSS classes
- `chore`: Zone.Identifier cleanup + .gitignore rule
- `build`: explicit research copy in package.json build script
- `copy`: AI/HPC → AI, HPC in Identity bio
- `feat(foundry)`: finalize Siliconization narrative and border-trace animation
